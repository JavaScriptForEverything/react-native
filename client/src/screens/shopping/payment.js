import { StyleSheet, View, Text } from 'react-native'

const PaymentScreen = ({ step }) => {
  // console.log({ step })

  return (
    <View>
      <Text>Payment</Text>
    </View>
  )
}
export default PaymentScreen

const styles = StyleSheet.create({

})